import { defaultOptions } from './config'

export default class Recorder {
    constructor(options) {
        //this.options = {...defaultOptions, options},
        this.render();
    }
    render() {

    }
}